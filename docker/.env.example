# Example environment variables for IBKR Gateway
# Copy this file to .env and fill in your actual values
# NEVER commit .env file with actual credentials!

# IBKR Credentials
IB_USER=your_username
IB_PASSWORD=your_password

# Trading Mode: 'paper' or 'live'
TRADING_MODE=paper

# TrustedIPs - comma-separated list of IPs/CIDRs
# Examples:
#   Single IP: 127.0.0.1
#   CIDR range: 172.20.0.0/16
#   Multiple: 127.0.0.1,172.20.0.0/16,10.0.0.0/8
TRUSTED_CIDRS=127.0.0.1,172.20.0.0/16

# Watchdog - set to 'true' to enable continuous monitoring
WATCHDOG_ENABLED=false

# 2FA Configuration
TWOFA_TIMEOUT_ACTION=exit

# Optional: TOTP Secret (base32 encoded)
# Only if you want automatic 2FA handling
# OTP_SECRET=YOUR_BASE32_SECRET_HERE
